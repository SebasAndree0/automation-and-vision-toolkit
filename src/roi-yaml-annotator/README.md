
---

# ✅ README 2 (ES/EN)


```md
README (Español / English)

# ROI YAML Annotator (GUI) — Editor GUI de ROIs en YAML

## Qué es / What is it
App de escritorio para dibujar ROIs (polígonos/rectángulos/círculos) y guardar/cargar YAML. — Desktop app to draw ROIs (polygons/rectangles/circles) and save/load YAML.

## Requisitos / Requirements
Python 3.9+ — Python 3.9+  
pyyaml, pillow — pyyaml, pillow  
Tkinter (Windows normalmente lo trae) — Tkinter (usually included on Windows)

## Instalación / Install
```bash
pip install pyyaml pillow